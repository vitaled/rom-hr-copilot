# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: code
metadata:
    template: azd-init@1.3.1
services:
    code:
        project: .
        host: containerapp
        language: python
        docker:
            path: Dockerfile
